```markdown
# ⚙️ AI 全自動化招募系統與戰情室：本地部署與實戰操作指南

本文件專為專案團隊組員、教授與助教設計。本專案採用「雙網頁並行」架構。請依循本指南之順序執行，即可在本地端重現整套 AI 招募與數據分析。

---

## 📋 運行環境要求與套件安裝

本專案採用相對路徑設計，請在執行前確保電腦已安裝 Python 3.9+，並於終端機（Terminal）一鍵安裝所有現代化依賴套件：

```bash
pip install -r requirements.txt

```

---

## 🔐 關鍵前置步驟：配置資安憑證與金鑰

基於企業級資安防護規範，敏感私鑰已透過 `.gitignore` 安全隔離於本地。請先完成以下設定：

1. **Google Cloud API 憑證 (`credentials.json`)**：
請將本地專案目錄下的 `credentials.json.example` 複製一份，並重新命名為 **`credentials.json`**，隨後填入您自 Google Cloud Console 申請的桌面版 OAuth 憑證金鑰。
2. **本地環境金鑰 (Secrets)**：
若於本地執行，請確保根目錄已建立 `.streamlit/secrets.toml` 檔案，並配置對應的 `GEMINI_API_KEY`、`SENDER_EMAIL` 與 `GMAIL_APP_PASSWORD`，以利系統調用雲端大腦與郵件引擎。

---

## 🎬 實戰操作與雲端體驗劇本

本專案已全面部署至 Streamlit Cloud，評審與助教可直接透過網頁體驗完整功能；若需於本地端進行代碼審查與測試，請依循以下劇本操作。

### 🌐 途徑一：雲端即時免安裝體驗（推薦 🚀）

不需配置任何本地環境，直接點擊下方連結即可進入已上線系統：

* **人資端控制台（後台 app.py）**：[點此進入系統](https://ntupbc-hr-dashboard.streamlit.app/)
* **應徵者預約端（前台 streamlit_app.py）**：[點此進入系統](https://ntupbc-interview-booking.streamlit.app/)

---

### 💻 途徑二：本地端原始碼審查與測試

若助教抗拒雲端環境、堅持在本地環境執行原始碼，請依序執行以下兩步：

#### 📡 步驟一：同步主管最新日曆空檔

在系統開放給應徵者預約前，必須先獲取雙主管（已預設是我們兩個組員的信箱）的公務忙碌行程。請於終端機執行：

```bash
python Calendar_BusyTime_Picker.py

```

* **產出結果**：首次執行會自動跳出瀏覽器進行 Google 帳號授權。隨後系統會自動完成時區對齊（GMT+8）並排除週末，於本地生成最新的 `calendar_output.json` 行程快取資料庫。

#### 🏢 步驟二：啟動前後台網頁服務

本系統已將 HR 後台控制台與前台預約系統完全解耦。您可以依測試需求，在終端機分別啟動它們：

* **啟動 HR 後台（控制台、AI 履歷解析、主管審核看板、郵件分流台、數據儀表板）**：
```bash
streamlit run app.py

```


* *操作說明*：進入網頁後，可於 Tab 1 上傳 `sample_resumes/` 資料夾內的 PDF 履歷進行 AI 初篩，並於各分頁體驗完整的招募有限狀態機流轉。


* **啟動應徵者前台（自主面試預約系統）**：
```bash
streamlit run streamlit_app.py

```


* *操作說明*：網頁會秒讀步驟一產出的 JSON 快取，動態計算出主管們交集的 15 分鐘緩衝空檔方塊。確認預約時，前台將自動呼叫 `calendar_sync.py` 後端模組，實時於主管日曆上生成 Google Meet 視訊會議。



---

## 🧹 測試重置與環境擦拭工具

為了方便團隊與助教進行多輪反覆流暢測試，專案附帶了一鍵清理機制：

* **日曆行程一鍵清空**：執行 `python clear_calendar_events.py`，輸入 `yes`，即可一鍵清除未來日曆上所有【面試】行程，主管日曆瞬間回歸乾淨初始狀態。
* **試算表數據清空**：直接開啟後台對接的 Google Sheets（應徵者追蹤表），將第 2 行以下的測試求職者欄位整列選取並刪除，即可重新開始新一輪的系統功能展示。

---

**最後更新**：2026-06-03

```

```

```